*.onnx
*.onnx.json
.candidate.html

# Personal Claude Code settings. .claude/settings.json IS committed — it is a
# fact about this project, not a preference — but settings.local.json and the
# worktrees a session creates are not.
.claude/settings.local.json
.claude/worktrees/
.claude/
